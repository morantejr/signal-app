"""SIGNAL research system.

Four separated stages: evidence (code), quant (code, never sees an LLM),
bull and bear memos (OpenRouter), synthesis with an explicit disagreement
object (OpenRouter + code). Everything is traced.
"""

__version__ = "0.1.0"
